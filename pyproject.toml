[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-asset-platform"
version = "3.3.0"
description = "AI asset management and automated trading platform"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
