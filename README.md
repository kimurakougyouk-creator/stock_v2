# stock_v2

AI Asset Platform development repository.

## Safety status

- IBKR Paper capability is verified only for the explicitly recorded bounded scope.
- Live Trading remains fail-closed unless a separate, explicit real-cash authorization and all readiness gates are satisfied.
- Read-only Live preparation code must not place, preview, modify, cancel, retry, or close orders.
- GitHub `main`, CI results, and broker/runtime evidence are authoritative over old chat text.

See `HANDOFF_MASTER.md` and `PROJECT_STATE.md` for the canonical project state and bounded evidence ledger.
